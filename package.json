{
  "name": "sigen-mcp",
  "version": "0.1.0",
  "private": true,
  "description": "Sigenergy ESS MCP servers — Modbus TCP + SigenCloud API",
  "workspaces": [
    "packages/modbus-mcp",
    "packages/api-mcp",
    "packages/sigen-docs-mcp"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "docs": "npm run docs -w packages/modbus-mcp",
    "clean": "npm run clean --workspaces 2>/dev/null; rm -rf node_modules packages/*/node_modules"
  },
  "keywords": [
    "mcp",
    "sigenergy",
    "modbus",
    "sigencloud",
    "sigen"
  ],
  "license": "MIT"
}
